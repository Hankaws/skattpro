// The root route is handled by the (marketing) route group.
// This file is intentionally empty / minimal so the beautiful landing at src/app/(marketing)/page.tsx renders at "/".
// Do not add redirects here.
export default function MarketingRoot() {
  return null;
}
